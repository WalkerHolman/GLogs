from datetime import datetime

# schema types for reference
SCHEMA = {
    "IS_DAY": float,
    "LOCATION": str,
    "PRECIPITATION": float,
    "PRECIPITATION_PROBABILITY": float,
    "TEMPERATURE": float,
    "TIME": datetime,
}
